{# app/Resources/views/security/login.html.twig #}
{# ... you will probably extends your base template, like base.html.twig #}

{% extends 'main.html.twig' %}

{% block title %}LogIn{% endblock %}

{%block stylesheets  %}
	<link rel="stylesheet" href="{{ asset('css/seller/common.css') }}" media="all" />
	<link rel="stylesheet" href="{{ asset('css/seller/header-nav.css') }}" />
	<link rel="stylesheet" href="{{ asset('css/seller/categories.css') }}" />
	<link rel="stylesheet" href="{{ asset('css/seller/login-registration.css') }}" media="all" />
{% endblock %}

{% block body %}

{{ render(controller('AppBundle:Seller:new')) }}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}

<div class="login common-block btn-style-href">
<h4>LogIn</h4>
	<form action="{{ path('login') }}" method="post">
		<!--<table>
			<tr>
				<td>	    
					<label for="username">Username:</label>
				</td>
				<td>-->	    
					<input placeholder="Name" type="text" id="username" name="_username" value="{{ last_username }}" />
				<!--</td>
			</tr>
			<tr>
				<td>	    
					<label for="password">Password:</label>
				</td>
				<td>-->	   
					<input placeholder="Password" type="password" id="password" name="_password" />
				<!--</td>
			</tr>
		</table>-->
	    {#
	        If you want to control the URL the user
	        is redirected to on success (more details below)
	        <input type="hidden" name="_target_path" value="/account" />
	    #}
	    <button type="submit">Login</button>
	</form>
</div>
{% endblock %}