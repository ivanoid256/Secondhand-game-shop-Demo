{# {% extends 'base.html.twig' %} 

{% block body %}#}
    <h3>Add Comment: </h3>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

{# 
        <input type="submit" value="Create" />
  <ul>
        <li>
            <a href="{{ path('comment_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}#}
