{% extends 'base.html.twig' %}

{% block body %}
    <h1>Image list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Srcref</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for image in images %}
            <tr>
                <td><a href="{{ path('seller_image_show', { 'id': image.id }) }}">{{ image.id }}</a></td>
                <td>{{ image.srcRef }}</td>
                <td>{{ image.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('seller_image_show', { 'id': image.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('seller_image_edit', { 'id': image.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        {#<li>
            <a href="{{ path('seller_image_new') }}">Create a new entry</a>
        </li>#}
    </ul>
{% endblock %}
